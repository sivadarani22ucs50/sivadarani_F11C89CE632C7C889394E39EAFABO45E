#3.1
def 
linearSearhProduct(productList, targetProduct):
  indices = []
  for index, product in enumerate(productList):
    indices.append(index)

return indices


#Example usage:
products = ["shoes", "boot", "loafer", "shoes", "sandal", "shoes"]
target-"shoes"
target2='apple'
result=linearSearchProduct(product, target)
print(result)